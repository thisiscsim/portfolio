import type React from "react"
// Check if this file exists and might be causing redirects

export default function WritingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
